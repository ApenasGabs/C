#include <stdio.h>

int main(){

int  n, soma, soma2,i;
;

printf("Forneca um valor: ");
scanf("%d", &n);

soma = 0;
soma2 = 0;

 for(i=1; i <= n; i++){
    if (i <= n){
      (soma2 = i + i-1);
         printf("\nResultado: %d / %d\n", i, soma2);

    }
    }
return 0;
}
